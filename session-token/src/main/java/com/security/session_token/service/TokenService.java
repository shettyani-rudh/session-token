package com.security.session_token.service;

import org.springframework.stereotype.Service;

/**
 * TokenService can hold more advanced logic (blacklisting, audit) in future.
 * Kept minimal for assignment.
 */
@Service
public class TokenService {
    // placeholder for token-related helpers (blacklist checks etc.)
}
